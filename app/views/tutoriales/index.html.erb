<p id="notice"><%= notice %></p>

<h1>Tutoriales</h1>

<table>
  <thead>
    <tr>
      <th>Nombre del tutorial  </th>
      <th>Descripción  </th>
      <th>Nivel de dificultad  </th>
      <th>Enlace  </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tutoriales.each do |tutoriale| %>
      <tr>
        <td><%= tutoriale.title %></td>
        <td><%= tutoriale.body %></td>
        <td><%= tutoriale.level %></td>
        <td><%= tutoriale.url %></td>
        <td><%= link_to '  Ver ', tutoriale %></td>
        <td><%= link_to '  Modificar ', edit_tutoriale_path(tutoriale) %></td>
        <td><%= link_to '  Eliminar ', tutoriale, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear tutorial', new_tutoriale_path %>
